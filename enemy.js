class Enemy {
    constructor(canvas, positionY, speed) {
      this.canvas = canvas;
      this.ctx = this.canvas.getContext("2d");

      this.size = //fill
      this.x = this.canvas.width;
      this.y = positionY;
  
      this.speed = speed;
    }
